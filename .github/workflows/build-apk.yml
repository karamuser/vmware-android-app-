name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      # إزالة مجلد أندرويد القديم إن وجد وإنشاؤه من جديد بأحدث معايير Flutter V2 Embedding
      - name: Re-generate Clean Android Project
        run: |
          rm -rf android
          flutter create . --platforms=android --org com.example

      # ضبط compileSdk 36 تلقائياً لمنع أخطاء المكتبات مثل file_picker
      - name: Patch compileSdk Version
        run: |
          sed -i 's/compileSdkVersion.*/compileSdkVersion 36/g' android/app/build.gradle || true
          echo "subprojects { afterEvaluate { project -> if (project.hasProperty('android')) { project.android { compileSdkVersion 36 } } } }" >> android/build.gradle

      - name: Fetch Flutter Packages
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: free-DROID-release
          path: build/app/outputs/flutter-apk/app-release.apk
          
